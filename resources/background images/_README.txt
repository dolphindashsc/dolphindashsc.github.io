This folder contains all of the background images that the page can load.
After updating the contents, always remember to run makenames again so 
allfiles.json is updated

allfiles is used by the script to load all of the images at load time
